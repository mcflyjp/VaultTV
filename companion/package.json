{
  "name": "vaulttv-companion",
  "version": "1.0.0",
  "description": "Local file-watcher companion server for VaultTV — auto-notifies the app when media files change",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
